import { DefaultThemeData } from '@/components/DefaultThemeData';
import { Layout } from '@/layout';
import { MDX_DATA } from '@/mdx';

export default Layout(MDX_DATA.DefaultTheme);

# Default theme object

## Import default theme object

You can import `DEFAULT_THEME` from `@bds-react/core` package:

```tsx
import { DEFAULT_THEME } from '@bds-react/core';
```

## Default theme object

<DefaultThemeData />
